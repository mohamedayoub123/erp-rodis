# Branche dev

Cette branche declenche un Vercel Preview Deployment connecte a la base de donnees V2 (dev/test), separee de la base de production (V1) utilisee par `main`.

A utiliser pour tester des changements avant de les fusionner dans `main`.
